name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Least privilege: this workflow only reads the repo to test it.
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -q

      - name: Validate example charters
        run: |
          agenraci validate examples/hello-world/charter.yaml
          agenraci validate examples/blog/charter.yaml
          agenraci validate examples/sprout/charter.yaml
          agenraci validate examples/relay/charter.yaml

      - name: Validate the starter template
        run: agenraci validate agenraci/templates/charter.template.yaml

      - name: Validate the project's own governance charter
        run: agenraci validate governance/charter.yaml
